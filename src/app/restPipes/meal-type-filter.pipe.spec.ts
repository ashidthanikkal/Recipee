import { MealTypeFilterPipe } from './meal-type-filter.pipe';

describe('MealTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MealTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
